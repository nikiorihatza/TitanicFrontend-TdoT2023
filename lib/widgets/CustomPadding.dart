import 'package:flutter/cupertino.dart';

class CustomPadding extends StatelessWidget {
  const CustomPadding({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 20,
    );
  }
}